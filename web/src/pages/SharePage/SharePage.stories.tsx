import type { Meta, StoryObj } from '@storybook/react'

import SharePage from './SharePage'

const meta: Meta<typeof SharePage> = {
  component: SharePage,
}

export default meta

type Story = StoryObj<typeof SharePage>

export const Primary: Story = {}
